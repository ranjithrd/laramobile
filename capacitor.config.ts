import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.laravel.app',
  appName: 'Laramobile',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
